# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['wrapper_build.py'],
    pathex=[],
    binaries=[],
    datas=[('C:/Users/bense/Desktop/Docs/ACCT 8211 Make up Assessment.pdf', '.'), ('dist_temp\\agent_core.exe', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='Support_Tool_Premium',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    uac_admin=True,
)
